spring:
  application:
    name: spring-guardian-server
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB

server:
  port: 8080

spring-guardian:
  api-prefix: "/api/v1"
  max-controller-lines: 35
  max-controller-branches: 8
